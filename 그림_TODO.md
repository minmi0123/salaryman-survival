# 그림 작업 체크리스트

> 전부 **1000×1000 정사각 PNG · 흑백** (OG만 1200×630 가로)
> 테두리는 CSS 가 그린다 — 그림에 테두리 넣지 말 것
> 표시 크기는 340~360px 이므로 **굵은 선 · 큰 형태**로
>
> **진행: 28개 중 28개 완료 ✅** — 인트로 3 · 타이틀 · 취침 · 엔딩 21
> 원안은 1000×560 가로였으나, 실제로 그려진 그림이 인물 중심이라 정사각으로 바꿨다.

## 켜는 법

- 타이틀·취침 → `index.html` 의 `IMG.title` / `IMG.sleep` 에 경로 입력
- 엔딩 21종 → `img/end/` 에 파일을 넣고 `IMG.endDir` 를 `'img/end/'` 로 변경 (한 줄)
  아직 안 그린 것은 `<img>` 가 스스로 사라지므로 하나씩 채워도 된다

---

## 0. 인트로 (3장) — `img/intro/` ✅ 완료

`새로하기` 를 누르면 [다음] [다음] [시작] 으로 넘어가는 프롤로그.
`IMG.intro` 배열이 장수를 결정한다. 빈 배열이면 인트로 없이 바로 시작.

- [x] `inst01.png` — 고된 취준 끝에…
- [x] `inst02.png` — 취직에 성공한 OO씨!
- [x] `inst03.png` — 과연 OO씨의 운명은?

> 런칭TODO §1-3 의 "D1 미통과 50% 초과 → 튜토리얼 필요" 는 **조작법** 안내를 말한다.
> 이 인트로는 이야기 프롤로그라 그 항목을 대체하지 않는다. 아직 열린 과제.

## 1. 게임 내부 (2장) — `img/`

- [x] `title.png` — 시작화면. 제목 아래, 메뉴 버튼 위
- [x] `sleep.png` — 취침 팝업. **가장 자주 보는 그림**
      문구 5종: 생명유지중… / 잠자는중… / 미래를 준비하는중… / 하루하루 살아가는중… / 먹고살걱정중…
      → "푹 잤다" 가 아니라 **버티려고 눕는다**

## 2. 배드엔딩 (4장) — 여기부터 ★

화면 대사가 **"안녕 나는 직장인이었던 유령인데 ○○여서 죽었어"** 다.
→ 4장 모두 **같은 유령 캐릭터**, 죽은 방식만 다르게. 통일감 + 작업량 절감.

- [x] `end18.png` 퇴사 — 멘탈 고갈 3회
- [x] `end19.png` 아사 — 건강 고갈 3회
- [x] `end20.png` 백수 — 월세 미납 2회
- [x] `end21.png` 짤림 — 업무 고갈 3회 또는 회사 경고 3회

> 1회차(스킬 0) 시뮬 12판이 **전부 퇴사**로 끝났다.
> 신규 유저가 처음 보는 엔딩이 사실상 `퇴사` 하나 → `end18.png` 의 효과가 가장 크다.

## 3. 트리 단독 엔딩 (8장)

- [x] `end01.png` 하극상 — 사장님 · "매출이 안나는데 자리 좀 비켜주시죠?"
- [x] `end02.png` 연금이나타자 — 팀장님 · "직장이 최고야..."
- [x] `end03.png` 직장인 왜하는데ㅋㅋ — 돈많은백수
- [x] `end04.png` 이제 이게 본업인데요? — 크리에이터
- [x] `end05.png` 제가 좀 잘 만들어요 — 검은손
- [x] `end06.png` 저도 이제 대표입니다 — 사업가
- [x] `end07.png` 회사는 헬스장 가는 길에 — 운동
- [x] `end08.png` 이 또한 지나가리라 — 기도

## 4. 조합 엔딩 (8장)

두 트리의 특징을 **한 그림에 섞는 것**이 핵심.

- [x] `end09.png` 꼰대임원 — (사장님·팀장님) × 운동
- [x] `end10.png` 헬스장사장님 — 크리에이터 × 운동
- [x] `end11.png` 치킨집사장님 — 돈많은백수 × 운동
- [x] `end12.png` 헬창조직보스 — (검은손·사업가) × 운동
- [x] `end13.png` 출근이 곧 수행 — (사장님·팀장님) × 기도
- [x] `end14.png` 구독자가 곧 신도 — 크리에이터 × 기도
- [x] `end15.png` 가진 게 없으니 잃을 것도 없다 — 돈많은백수 × 기도
- [x] `end16.png` 이거 드시고 구원받으세요 — (검은손·사업가) × 기도

## 5. 특수 (1장)

- [x] `end17.png` 건물주 — 주말 상점에서 부동산 구매(30만). 돈으로만 도달하는 유일한 엔딩

## 6. 런칭용

- [x] `og.png` (1200×630) — **title.png 재사용**(손글씨 제목이 이미 들어있음). — **그림 안에 제목 글자가 박혀야 한다.** 카톡·X 는 이미지만 크게 뜬다
      런칭TODO: "엔딩 이름을 미끼로" → 자극적인 이름 활용
- [x] favicon — 💼 이모지를 SVG data URI 로 처리. 그림 불필요. **이모지로 대체 가능** (런칭TODO 명시). 부담되면 생략

---

## 절대 하지 말 것

- `이미지/` 폴더(참고작 Sweatshop diary 스크린샷)를 **베끼지 말 것.** 톤 참고만.
  이 repo 는 Public 이다. 구도·캐릭터를 따라 그리면 배포 후 문제가 된다.
- 파일명에 **한글 금지.** 맥은 자모 분리(NFD), 서버는 완성형(NFC)이라
  로컬에선 보이는데 배포하면 404 가 난다. `endNN.png` 를 쓰는 이유다.

---

# TODO — 블록 그림을 스킬 투자에 따라 바꾸기

한 블록에 그림을 여러 장 두고, **어느 계열에 SP 를 넣었는지에 따라 다른 그림**이 뜨게 한다.
예) 자기관리를 많이 찍었으면 `출근준비` 가 잘 챙겨입고 나가는 그림으로 바뀐다.

## 가능 여부 — 가능. 난이도 낮음

이미 있는 것:

```js
계열SP('자기관리관련')   // 그 계열에 투자한 SP 총합 (index.html 665줄)
```

계열은 정확히 **4개** — `업무관련` · `부업알바관련` · `아이템관련` · `자기관리관련`.
"출근준비 그림 4장" 과 그대로 대응된다.

고칠 곳은 `blockImgTag()` **한 군데뿐**이다. 지금은 블록당 파일 하나를 고르는데,
거기서 계열SP 를 읽어 고르게만 바꾸면 된다. 구조 변경 없음.

## 정해야 할 것

**1) 어떤 규칙으로 고르나**

| 안 | 규칙 | 장단 |
|---|---|---|
| A. 우세 계열 | 4개 중 SP 가 가장 많은 계열의 그림 | 항상 1장이 정해진다. **전부 0인 1회차 초반용 기본 그림이 따로 필요**(블록당 5장) |
| B. 임계값 | 특정 계열이 N 이상이면 그 그림 | 기본 그림을 안 그려도 된다. 여러 계열이 동시에 넘으면 **우선순위를 정해야 함** |

A 가 규칙이 단순하고 "내가 뭘 키웠는지" 가 그림에 드러나 환생 반복형과 잘 맞는다.
다만 1회차(SP 0)에 뭘 보여줄지 결정 필요 — 지금 그림을 기본으로 쓰면 4장만 더 그리면 된다.

**2) 어느 블록에 적용하나 — 작업량 문제**

블록 그림은 8장(평일 6 + 토 + 일)이다. 전부에 4단계를 넣으면 **8 × 4 = 32장**을 더 그려야 한다.
일부만 적용해도 되게 만들 것 — 변형이 없는 블록은 지금처럼 한 장을 쓴다.

체감이 큰 블록부터 고르는 편이 낫다. 후보:
- `출근준비` — 자기관리가 가장 잘 드러난다
- `오후업무` — 업무관련/부업알바 차이가 드러난다
- `저녁` · 주말 — 부업·자기관리 차이가 드러난다

**3) 파일명 규칙**

`blk01.png` → `blk01_0.png`(기본) · `blk01_1.png`(업무) · `blk01_2.png`(부업알바) …
한글 파일명은 쓰지 않는다(NFD/NFC 문제).

**4) 미리받기 대상**

지금 블록 8장을 부팅 후 미리 받는다. 변형이 늘면 **현재 상태에 해당하는 것만** 받도록
바꿔야 한다. 32장을 다 받으면 모바일 데이터가 아깝다.

## 착수 전 확인

- [ ] 규칙 A / B 중 선택
- [ ] 적용할 블록 선정 (전부? 일부?)
- [ ] 1회차 기본 그림 처리 방법

---

# TODO — 랜덤 이벤트 그림 14장 (보류)

2026-09-11 랜덤 이벤트가 들어갔다. 지금은 글자만 뜬다.

**난이도: 낮음.** `IMG.evDir` 스위치가 이미 열려 있어서, 그림을 넣고 경로만 채우면 된다.
안 그린 것은 `<img>` 가 스스로 사라지므로 **하나씩 채워도 된다** (엔딩 21종과 같은 방식).

```
const IMG = { ..., evDir: '' };   // → 'img/ev/' 로 바꾸면 한 번에 살아난다
```

## 파일명 — EVENTS 의 id 를 그대로 쓴다

규격은 엔딩과 같은 **1000x1000 정사각**. `./그림변환.sh` 를 그대로 쓸 수 있다.

| 파일 | 이벤트 | 부/긍 | 장면 |
|---|---|:--:|---|
| `order.png` | 갑작스런 지시 | ✕ | 팀장님이 할 말만 하고 뒤돌아 가는 뒷모습 |
| `cancel.png` | 회의 취소 | ○ | 취소 알림이 뜬 화면, 속으로 환호 |
| `quit.png` | 옆자리 퇴사 | ✕ | 텅 빈 옆자리, 쌓인 서류가 이쪽으로 |
| `rookie.png` | 신입 들어옴 | ○ | 90도로 인사하는 신입 |
| `exec.png` | 엘리베이터에서 임원 | ✕ | 단둘이 탄 엘리베이터, 층수 표시 |
| `snack.png` | 탕비실 신상 간식 | ○ | 과자 두 개 챙기는 손 |
| `ot.png` | 야근 강요 | ✕ | 6시 넘었는데 아무도 안 일어남, 가방 든 채 |
| `ricecake.png` | 고객사가 떡을 돌렸다! | ○ | 떡 상자 |
| `roach.png` | 눈을 떴는데 (바퀴벌레) | ✕ | 누운 시점에서 본 바퀴벌레 |
| `sun.png` | 눈을 떴는데 (햇빛) | ○ | 누운 시점에서 본 창문 햇살 |
| `cold.png` | 감기 | ✕ | 이불 뒤집어쓰고 코 훌쩍 |
| `mom.png` | 엄마가 반찬을 보내줬다! | ○ | 문 앞 아이스박스 |
| `wedding.png` | 경조사 | ✕ | 청첩장과 봉투 |
| `resell.png` | 중고거래 대박 | ○ | 팔렸다는 알림 |

## 그릴 때 주의

- **`roach` 와 `sun` 은 짝이다.** 팝업 제목이 `눈을 떴는데` 로 같고 본문에서 갈린다.
  **구도를 똑같이 맞추고 창문 쪽만 바꾸면** 뒤집히는 맛이 산다. 이 둘은 같이 그릴 것.
- 부정/긍정이 축마다 짝이라, 짝끼리 톤을 맞추면 대비가 산다
  (`order`↔`cancel`, `quit`↔`rookie`, `exec`↔`snack`, `ot`↔`ricecake`,
   `cold`↔`mom`, `wedding`↔`resell`).

## 착수 전 확인

- [ ] 14장을 다 그릴지, 체감 큰 것부터 몇 장만 할지
- [ ] 미리받기에 넣을지 — 이벤트는 확률로 뜨므로 14장을 다 받으면 모바일 데이터가 아깝다.
      지금은 미리받기 대상이 아니다 (뜰 때 받는다)
