# 그림 작업 체크리스트

> 전부 **1000×1000 정사각 PNG · 흑백** (OG만 1200×630 가로)
> 테두리는 CSS 가 그린다 — 그림에 테두리 넣지 말 것
> 표시 크기는 340~360px 이므로 **굵은 선 · 큰 형태**로
>
> **진행: 28개 중 28개 완료 ✅** — 인트로 3 · 타이틀 · 취침 · 엔딩 21
> 원안은 1000×560 가로였으나, 실제로 그려진 그림이 인물 중심이라 정사각으로 바꿨다.

## 켜는 법

- 타이틀·취침 → `index.html` 의 `IMG.title` / `IMG.sleep` 에 경로 입력
- 엔딩 21종 → `img/end/` 에 파일을 넣고 `IMG.endDir` 를 `'img/end/'` 로 변경 (한 줄)
  아직 안 그린 것은 `<img>` 가 스스로 사라지므로 하나씩 채워도 된다

---

## 0. 인트로 (3장) — `img/intro/` ✅ 완료

`새로하기` 를 누르면 [다음] [다음] [시작] 으로 넘어가는 프롤로그.
`IMG.intro` 배열이 장수를 결정한다. 빈 배열이면 인트로 없이 바로 시작.

- [x] `inst01.png` — 고된 취준 끝에…
- [x] `inst02.png` — 취직에 성공한 OO씨!
- [x] `inst03.png` — 과연 OO씨의 운명은?

> 런칭TODO §1-3 의 "D1 미통과 50% 초과 → 튜토리얼 필요" 는 **조작법** 안내를 말한다.
> 이 인트로는 이야기 프롤로그라 그 항목을 대체하지 않는다. 아직 열린 과제.

## 1. 게임 내부 (2장) — `img/`

- [x] `title.png` — 시작화면. 제목 아래, 메뉴 버튼 위
- [x] `sleep.png` — 취침 팝업. **가장 자주 보는 그림**
      문구 5종: 생명유지중… / 잠자는중… / 미래를 준비하는중… / 하루하루 살아가는중… / 먹고살걱정중…
      → "푹 잤다" 가 아니라 **버티려고 눕는다**

## 2. 배드엔딩 (4장) — 여기부터 ★

화면 대사가 **"안녕 나는 직장인이었던 유령인데 ○○여서 죽었어"** 다.
→ 4장 모두 **같은 유령 캐릭터**, 죽은 방식만 다르게. 통일감 + 작업량 절감.

- [x] `end18.png` 퇴사 — 멘탈 고갈 3회
- [x] `end19.png` 아사 — 건강 고갈 3회
- [x] `end20.png` 백수 — 월세 미납 2회
- [x] `end21.png` 짤림 — 업무 고갈 3회 또는 회사 경고 3회

> 1회차(스킬 0) 시뮬 12판이 **전부 퇴사**로 끝났다.
> 신규 유저가 처음 보는 엔딩이 사실상 `퇴사` 하나 → `end18.png` 의 효과가 가장 크다.

## 3. 트리 단독 엔딩 (8장)

- [x] `end01.png` 하극상 — 사장님 · "매출이 안나는데 자리 좀 비켜주시죠?"
- [x] `end02.png` 연금이나타자 — 팀장님 · "직장이 최고야..."
- [x] `end03.png` 직장인 왜하는데ㅋㅋ — 돈많은백수
- [x] `end04.png` 이제 이게 본업인데요? — 크리에이터
- [x] `end05.png` 제가 좀 잘 만들어요 — 검은손
- [x] `end06.png` 저도 이제 대표입니다 — 사업가
- [x] `end07.png` 회사는 헬스장 가는 길에 — 운동
- [x] `end08.png` 이 또한 지나가리라 — 기도

## 4. 조합 엔딩 (8장)

두 트리의 특징을 **한 그림에 섞는 것**이 핵심.

- [x] `end09.png` 꼰대임원 — (사장님·팀장님) × 운동
- [x] `end10.png` 헬스장사장님 — 크리에이터 × 운동
- [x] `end11.png` 치킨집사장님 — 돈많은백수 × 운동
- [x] `end12.png` 헬창조직보스 — (검은손·사업가) × 운동
- [x] `end13.png` 출근이 곧 수행 — (사장님·팀장님) × 기도
- [x] `end14.png` 구독자가 곧 신도 — 크리에이터 × 기도
- [x] `end15.png` 가진 게 없으니 잃을 것도 없다 — 돈많은백수 × 기도
- [x] `end16.png` 이거 드시고 구원받으세요 — (검은손·사업가) × 기도

## 5. 특수 (1장)

- [x] `end17.png` 건물주 — 주말 상점에서 부동산 구매(30만). 돈으로만 도달하는 유일한 엔딩

## 6. 런칭용

- [x] `og.png` (1200×630) — **title.png 재사용**(손글씨 제목이 이미 들어있음). — **그림 안에 제목 글자가 박혀야 한다.** 카톡·X 는 이미지만 크게 뜬다
      런칭TODO: "엔딩 이름을 미끼로" → 자극적인 이름 활용
- [x] favicon — 💼 이모지를 SVG data URI 로 처리. 그림 불필요. **이모지로 대체 가능** (런칭TODO 명시). 부담되면 생략

---

## 절대 하지 말 것

- `이미지/` 폴더(참고작 Sweatshop diary 스크린샷)를 **베끼지 말 것.** 톤 참고만.
  이 repo 는 Public 이다. 구도·캐릭터를 따라 그리면 배포 후 문제가 된다.
- 파일명에 **한글 금지.** 맥은 자모 분리(NFD), 서버는 완성형(NFC)이라
  로컬에선 보이는데 배포하면 404 가 난다. `endNN.png` 를 쓰는 이유다.
